# Reserved for Phase 2. The reward and GRPO math modules are already shared with SFT evaluation.
model_name_or_path: Qwen/Qwen2.5-0.5B-Instruct
seed: 42
num_generations: 4
max_prompt_length: 256
max_completion_length: 128
per_device_train_batch_size: 1
gradient_accumulation_steps: 1
learning_rate: 0.00001
beta: 0.04
reward_answer: 1.0
reward_format: 0.1
use_vllm: false

