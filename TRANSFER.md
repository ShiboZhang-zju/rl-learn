# Transfer / Resume

The old transfer guide is deprecated.

For the current experiment state and GPU recovery procedure, read:

`EXPERIMENT_STATE.md`

New agents should always start from that file.

On a new GPU machine:

```bash
bash scripts/bootstrap_gpu.sh
```
